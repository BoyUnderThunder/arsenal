# Arsenal kernel hardening (sysctl). Conservative defaults that don't get in
# the way of security tooling.

# Hide kernel pointers and restrict dmesg to root.
kernel.kptr_restrict = 2
kernel.dmesg_restrict = 1

# Restrict ptrace to child processes only (debuggers still work on what you spawn).
kernel.yama.ptrace_scope = 1

# Restrict loading of unprivileged BPF and harden JIT.
kernel.unprivileged_bpf_disabled = 1
net.core.bpf_jit_harden = 2

# Restrict kernel performance events.
kernel.perf_event_paranoid = 3

# Don't let regular users dump SUID core files.
fs.suid_dumpable = 0

# Protect hardlinks/symlinks and FIFOs/regular files in world-writable dirs.
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2

# Network: ignore source routing & redirects, log martians, enable rp_filter.
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.all.log_martians = 1
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.all.accept_source_route = 0
net.ipv4.tcp_syncookies = 1
