#!/usr/bin/env bash
# Arsenal platform CLI launcher.
# Dispatches to the Python implementation installed at /usr/lib/arsenal.
# `arsenal` with no arguments prints the armory (backwards compatible).
exec env PYTHONPATH="/usr/lib/arsenal${PYTHONPATH:+:${PYTHONPATH}}" python -m arsenal_cli "$@"
